<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".Provocador_de_anr" >

    <Button
        android:id="@+id/btnSinHilos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/textView1"
        android:layout_alignParentTop="true"
        android:layout_marginTop="30dp"
        android:onClick="onClickBtnSinHilos"
        android:text="@android:string/ok" />

    <ProgressBar
        android:id="@+id/pbarProgreso"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/btnSinHilos"
        android:layout_alignTop="@+id/btnSinHilos"
        android:layout_marginLeft="20dp"
        android:layout_toRightOf="@+id/btnSinHilos" />

    <TextView
        android:id="@+id/textView1"
        style="@style/styleTextoSimple"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnSinHilos"
        android:layout_marginTop="23dp"
        android:text="@string/thread_desventajas" />

</RelativeLayout>